stage('Deployment on test') {
    steps {
        script {
            env.WAR_NAME = sh(script: "python ./main.py ${customerProfileId} ${FORCE_CUSTOMER_NAME}", returnStdout: true).toString().trim()
        }
    }
}